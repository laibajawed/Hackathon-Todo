🎉 Todo Chatbot Application has been deployed!

Your release is named {{ .Release.Name }}.

To access your application:

{{- if .Values.ingress.enabled }}

1. Ingress is enabled. Access your application at:
{{- range $host := .Values.ingress.hosts }}
  - Frontend: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/
  - Backend API: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/api
  - API Docs: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/docs
{{- end }}

{{- else }}

1. Get the frontend URL by running:
{{- if contains "NodePort" .Values.frontend.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "todo-chatbot.fullname" . }}-frontend)
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "Frontend: http://$NODE_IP:$NODE_PORT"
{{- else if contains "LoadBalancer" .Values.frontend.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "todo-chatbot.fullname" . }}-frontend --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo "Frontend: http://$SERVICE_IP:{{ .Values.frontend.service.port }}"
{{- else if contains "ClusterIP" .Values.frontend.service.type }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "todo-chatbot.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }}
  echo "Frontend: http://127.0.0.1:{{ .Values.frontend.service.port }}"
{{- end }}

2. Get the backend API URL by running:
{{- if contains "NodePort" .Values.backend.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "todo-chatbot.fullname" . }}-backend)
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "Backend API: http://$NODE_IP:$NODE_PORT"
  echo "API Docs: http://$NODE_IP:$NODE_PORT/docs"
{{- else if contains "LoadBalancer" .Values.backend.service.type }}
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "todo-chatbot.fullname" . }}-backend --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo "Backend API: http://$SERVICE_IP:{{ .Values.backend.service.port }}"
  echo "API Docs: http://$SERVICE_IP:{{ .Values.backend.service.port }}/docs"
{{- else if contains "ClusterIP" .Values.backend.service.type }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "todo-chatbot.fullname" . }}-backend {{ .Values.backend.service.port }}:{{ .Values.backend.service.port }}
  echo "Backend API: http://127.0.0.1:{{ .Values.backend.service.port }}"
  echo "API Docs: http://127.0.0.1:{{ .Values.backend.service.port }}/docs"
{{- end }}
{{- end }}

📝 Quick Start:

1. Check deployment status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. View logs:
   # Backend logs
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=backend -f

   # Frontend logs
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=frontend -f

3. Access the application:
   - Frontend UI: Access via the URL above
   - Backend API Docs: Add /docs to the backend URL

⚠️  Important Notes:

- Make sure your database is accessible from the cluster
- Update secrets in values.yaml or use --set flags for production
- Default credentials are for development only - change them in production!

For more information, visit: https://github.com/your-repo/todo-chatbot
